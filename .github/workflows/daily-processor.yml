name: Daily Stenogram Processor

on:
  schedule:
    - cron: '0 6 * * *'  # 06:00 UTC = 09:00 EET
  workflow_dispatch:
    inputs:
      force_refresh:
        description: 'Force full refresh'
        required: false
        default: 'false'

jobs:
  process:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run CDEP Agent
        run: |
          python scripts/agents/cdep_agent.py --years 2024,2025,2026 --max-id 50 --sync-vault
        continue-on-error: true

      - name: Run Senate Agent
        run: |
          python scripts/agents/senat_agent.py --year 2026 --max 10 --sync-vault
        continue-on-error: true

      - name: Run Master Controller (merge)
        run: |
          python scripts/stenomd_master.py --merge
        continue-on-error: true

      - name: Check status
        run: |
          python scripts/stenomd_master.py --status

      - name: Commit changes
        if: github.event_name == 'schedule'
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Daily update: $(date +%Y-%m-%d)"
          git push || echo "No changes to commit"

  notify:
    needs: process
    runs-on: ubuntu-latest
    if: failure()
    
    steps:
      - name: Notify failure
        run: |
          echo "StenoMD daily processing failed"
          echo "Check logs at: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"