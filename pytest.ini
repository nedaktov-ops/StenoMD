[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short --strict-markers
markers =
    agents: tests for scraper agents
    kg: tests for knowledge graph
    analyze: tests for analysis modules
    resolve: tests for entity resolution
